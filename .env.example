# Supabase project dùng chung với nhatkytrading + các module jp-go khác
# (bảng jp_vocab/jp_kanji/jp_grammar/... đã có sẵn ở đây). Module ôn thi
# 2級電気工事施工管理 chỉ thêm các bảng jp_exam_* - không đụng dữ liệu khác.
#
# URL và anon/publishable key KHÔNG phải bí mật (được thiết kế để chạy trong
# trình duyệt, RLS đã giới hạn chỉ SELECT công khai), nên điền sẵn giá trị
# thật ở đây cho tiện chạy `npm run dev` ngay.
NEXT_PUBLIC_SUPABASE_URL=https://frqujwlswqmtsxnqnwwc.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_TCG4KliEaKshVW9BKQJiCQ_1UrLbjdC

# Chỉ dùng cho `npm run exam:import` (chạy ở máy/CI, KHÔNG dùng trong code
# app/UI, KHÔNG commit giá trị thật). Lấy trong Supabase Dashboard > Project
# Settings > API > service_role secret key.
SUPABASE_SERVICE_ROLE_KEY=
